thm (x->y) -> (z->x) -> (z->y) as Thm1
proof
  load "axioms2" unless Ax5
  load "self" unless self

  inst self with a=z->x
  pop from #1
  pop from #2

  inst self with a=x->y
  pop from #4

  mp #3 and #5
  push z from #6
  push z->x from #7
  push x->y from #8 as goal
end

